import { LimitarDecimalesDirective } from './limitar-decimales.directive';

describe('LimitarDecimalesDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitarDecimalesDirective();
    expect(directive).toBeTruthy();
  });
});
