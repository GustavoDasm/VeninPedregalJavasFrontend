import { EstrategiaLiberacion } from './estrategia-liberacion';

describe('EstrategiaLiberacion', () => {
  it('should create an instance', () => {
    expect(new EstrategiaLiberacion()).toBeTruthy();
  });
});
