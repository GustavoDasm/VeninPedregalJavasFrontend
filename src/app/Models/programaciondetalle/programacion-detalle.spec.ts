import { ProgramacionDetalle } from './programacion-detalle';

describe('ProgramacionDetalle', () => {
  it('should create an instance', () => {
    expect(new ProgramacionDetalle()).toBeTruthy();
  });
});
